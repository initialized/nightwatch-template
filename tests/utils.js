// Put shared functions here (e.g., "login before running tests")

// A Simple convenience function that closes the browser after
// a test suite is run
export const endTests = (browser) => browser.end()
